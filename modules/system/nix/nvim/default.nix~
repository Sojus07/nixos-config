{
  pkgs,
  lib,
  ...
}: {
  imports = [
    ./lsp.nix
    ./terminal.nix
    ./neotree.nix
    ./opts.nix
    ./cmp.nix
  ];
  programs.nvf = {
    enable = true;
    settings = {
      vim = {
        extraLuaFiles = [
          (builtins.path {
            path = ./raw/opts.lua;
            name = "opts.lua"
          })
        ];
        autopairs.nvim-autopairs.enable = true;
        statusline.lualine = {
          enable = true;
        };
        telescope.enable = true;
        theme = {
          enable = true;
        };
      };
    };
  };
}

